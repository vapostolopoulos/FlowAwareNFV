unix { cli-listen /run/vpp/cli-vpp16.sock
	pidfile /run/vpp/vpp16.pid
}
api-segment { prefix vpp16 }
cpu {	main-core 31
	thread-prefix vpp16
}
plugins { plugin dpdk_plugin.so { disable } }
statseg { socket-name /run/vpp/stats-vpp16.sock }
